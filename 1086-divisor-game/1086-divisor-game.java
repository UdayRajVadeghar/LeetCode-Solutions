class Solution {
    public boolean divisorGame(int n) {

        return n%2 ==0;
        //Here we can understand the pattern and say that if it is even then alice would win it else bob could win it. This can be solved usingd dp aswell;
        
    }
}